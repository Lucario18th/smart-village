<UserControl x:Class="SmartVillageWPF.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SmartVillageWPF.ViewModels"
             d:DataContext="{d:DesignInstance vm:ConnectionViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="MQTT Connection" Style="{StaticResource HeadlineStyle}" />

            <!-- Connection Status Card -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Broker Connection" Style="{StaticResource TitleStyle}" Margin="0,0,0,12" />

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Broker:" Style="{StaticResource SubtitleStyle}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding BrokerInfo}"
                                   Style="{StaticResource BodyStyle}" FontWeight="SemiBold" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Status:" Style="{StaticResource SubtitleStyle}"
                                   Margin="0,4,0,0" />
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource StatusUnknownBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Connected">
                                                <Setter Property="Fill" Value="{StaticResource StatusOkBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Connecting">
                                                <Setter Property="Fill" Value="{StaticResource StatusWarnBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Reconnecting">
                                                <Setter Property="Fill" Value="{StaticResource StatusWarnBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding StatusText}" Value="Error">
                                                <Setter Property="Fill" Value="{StaticResource StatusErrorBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding StatusText}" Style="{StaticResource BodyStyle}" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <Button Content="Connect" Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding ConnectCommand}" Margin="0,0,8,0" />
                        <Button Content="Disconnect" Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding DisconnectCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- MQTT Topics Info Card -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Subscribed Topics" Style="{StaticResource TitleStyle}" Margin="0,0,0,8" />

                    <TextBlock Style="{StaticResource CaptionStyle}" Margin="0,2">
                        <Run Text="📡 Sensor Data: " FontWeight="SemiBold" />
                        <Run Text="sv/+/+/sensors/+" />
                    </TextBlock>
                    <TextBlock Style="{StaticResource CaptionStyle}" Margin="0,2">
                        <Run Text="🔍 Discovery: " FontWeight="SemiBold" />
                        <Run Text="sv/+/+/config" />
                    </TextBlock>
                    <TextBlock Style="{StaticResource CaptionStyle}" Margin="0,2">
                        <Run Text="📱 App Sensors: " FontWeight="SemiBold" />
                        <Run Text="app/village/+/sensors" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Log Messages -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Connection Log" Style="{StaticResource TitleStyle}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding LogMessages}" MaxHeight="300"
                             BorderThickness="0" Background="Transparent"
                             FontFamily="Consolas" FontSize="12" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
